import React from 'react'

import StickyNote from '../../components/Notes/StickyNote/StickyNote'

const Note = () => {
	return (
		<StickyNote
			titlePlaceholder='Note Title'
			bodyPlaceholder='So, what are you upto?'
		/>
	)
}

export default Note